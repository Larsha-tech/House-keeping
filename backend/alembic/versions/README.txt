# Alembic migration files go here.
# Generate your first migration with:
#   docker compose exec backend alembic revision --autogenerate -m "init"
# Apply with:
#   docker compose exec backend alembic upgrade head
